<div class="note-card-small">
	<a href="/note/<%- note.id %>" class="link"></a>
	<% if(note.thumbnailURL) { %>
		<div class="thumbnail">
			<img src="<%- note.thumbnailURL %>"/>
		</div>
	<% } %>
	<div class="meta-data">
		<div class="title"><%= note.title %></div>
		<div class="author"><%= note.authorDisplayName %><% if(note.isAuthorVerified) { %><span class="material-symbols-outlined">check</span><% } %></div>
		<div class="details">
			<span class="date"><%= $relativeTimeFormatter(note.createdAt) %></span>
			<span class="views"><%= note.views %> views</span>
			<% if(note.rateCount >= MIN_RATING_COUNT_TO_SHOW) { %>
				<span class="rating"><%= note.rating %> <span class="material-symbols-outlined">star</span></span>
			<% } %>
		</div>
		<% if(note.keywords.length > 0) { %>
			<div class="keyword-container">
				<% for(let j = 0; j < note.keywords.length; j++) { %>
					<label class="chip keyword"><a href="/explore?keyword=<%- note.keywords[j] %>"><%= note.keywords[j] %></a></label>
				<% } %>
			</div>
		<% } %>
	</div>
</div>